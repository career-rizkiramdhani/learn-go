# Models Module Context

Purpose: GORM model definitions used by the infrastructure layer.

Key files:
- `models/user.go` - GORM model for `User` with audit fields.

Notes:
- Models are ORM-specific and consumed by `infrastructure`.
