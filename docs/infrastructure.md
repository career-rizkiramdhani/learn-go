# Infrastructure Module Context

Purpose: concrete implementations for persistence, external services, and other I/O.

Key folders:
- `infrastructure/persistence` - GORM repositories (e.g., `gorm_user_repository.go`)

Notes:
- Implement interfaces defined in `domain/*` here.
