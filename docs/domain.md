# Domain Module Context

Purpose: domain entities and interfaces for DDD.

Key folders:
- `domain/` contains domain packages (e.g., `domain/user`).

Notes:
- Keep domain pure: no imports from `infrastructure` or `application`.
