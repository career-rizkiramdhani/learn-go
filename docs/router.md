# Router Module Context

Purpose: route registration and middleware setup.

Key files:
- `router/root.go` - registers routes under `/api`.

Notes:
- Keep routing simple; handlers implement business logic via `application` layer.
