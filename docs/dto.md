# DTO Module Context

Purpose: request and response payload structures used by handlers.

Key files:
- `dto/user.go` contains `CreateUserRequest`, `UpdateUserRequest`, and `UserResponse`.

Notes:
- Keep DTOs free of business logic.
